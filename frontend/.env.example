# Point the React app at your Django backend.
# Leave it unset to talk to http://localhost:8000 in dev, or to the same
# origin in production (single-service deploy).
VITE_API_URL=http://localhost:8000
